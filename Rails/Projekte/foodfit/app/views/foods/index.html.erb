  <div class="right2">
  <%= image_tag("food.jpg", size: "500") %>
  </div>

<div style="width:65%; height: 728px;">
<h1>Lebensmittel</h1>

<table border="2" class="table1">
  <thead>
    <tr>
      <th>Name</th>
      <th>Menge</th>
      <th>Einheit</th>
      <th>Eiweiß</th>
      <th>Fett</th>
      <th>Kohlenhydrate</th>
      <th>Kalorien</th>
      <th colspan="4" style="text-align:center">Optionen</th>
    </tr>
  </thead>

  <tbody>
    <% @foods.each do |food| %>
      <tr>
        <td><%= food.name %></td>
        <td><%= food.menge %></td>
        <td><%= food.einheit %></td>
        <td><%= food.eiweiß %></td>
        <td><%= food.fett %></td>
        <td><%= food.kohlenhydrate %></td>
        <td><%= food.kalorien %></td>
        <td><%= link_to 'Zum Tagebuch hinzufügen', new_food_fooddiary_path(food.id), class: 'success2 button' %></td>
        <td><%= link_to 'Bearbeiten', edit_food_path(food.id), class: 'warning button' %></td>
        <% if current_user_admin? %>
          <td><%= link_to 'Löschen', food_path(food.id), class: 'alert button', method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Lebensmittel hinzufügen', new_food_path, class: 'primary button' %>

<% ary0 = Body.all.collect { |s| s.weigth } %>

<%= javascript_include_tag :defaults %>

<script type="text/javascript">

var chart = AmCharts.makeChart( "chartdiv", {
  "type": "pie",
  "color": "white",
  "theme": "light",
  "dataProvider": [ {
    "antwort": "Trifft voll zu",
    "Anzahl": <%= ary0[0] %>
  }, {
    "antwort": "Trifft überwiegend zu",
    "Anzahl": <%= ary0[1] %>
  }, {
    "antwort": "Trifft teilweise zu",
    "Anzahl": <%= ary0[2] %>
  }, {
    "antwort": "Trifft nicht zu",
    "Anzahl": <%= ary0[3] %>
  } ],
  "valueField": "Anzahl",
  "titleField": "antwort",
   "balloon":{
   "fixedPosition":true
  },
  "export": {
    "enabled": true
  }
} );

</script>

</div>

